# Agent Notes

- Prefer deterministic tool arguments over vague calls.
- When a task needs multiple steps, call tools iteratively and then provide one final response.
- If a step fails, explain what failed and continue only when useful.
