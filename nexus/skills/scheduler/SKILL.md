# Scheduler Skill

Tool name: `scheduler`

Actions:
- `schedule`
- `list`
- `update`
- `cancel`

Guidelines:
- Confirm recurring cadence text is specific.
- Use `list` before `update`/`cancel` if job id is unclear.
- Keep reminder text short and unambiguous.
