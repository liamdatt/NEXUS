# Calendar Skill

Tool name: `calendar`

Action:
- `create_event`

Guidelines:
- Resolve clear start/end (or duration).
- Prefer local timezone unless user specifies another timezone.
- Include concise title and optional location/description/attendees.
