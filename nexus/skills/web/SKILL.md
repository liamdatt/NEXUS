# Web Skill

Tool name: `web`

Actions:
- `search_web` for discovery
- `fetch_url` for content extraction

Guidelines:
- Use targeted queries.
- Prefer one fetch after search instead of many broad fetches.
- Summarize findings clearly with source context where possible.
